// #include <Arduino.h>
// #include "FS.h"
// #include <LITTLEFS.h>

// #include <LittleDB.h>

void setup() {
  // Serial.begin(115200);
  // LITTLEFS.begin(true);
  // delay(5000);
  // LITTLEFS.format();
  // execQuery("create db test1");
  // execQuery("use db test1");
  
  // String schem = "id id, age int, ready tinyint, name text, zip int, room text";
  // schem.trim();
  // execQuery("create table test_tbl (" + schem + ")");
}

void loop() { }